# ── Core LLM / orchestration ──────────────────────────────────────────────────
# langchain-core is deliberately held at 0.3.x. Verified 2026-09-03:
#
#   ragas 0.4.3 (the latest release) hard-imports
#     langchain_community.chat_models.vertexai   -> ragas/llms/base.py:11
#   That module was removed in langchain-community 0.4.x, which is the only
#   community line compatible with langchain-core 1.x. The import is unguarded,
#   so `from ragas import evaluate` fails outright and the whole evaluation
#   pipeline goes with it.
#
#   Upgrading is also unnecessary: langgraph 1.0.1 runs on core 0.3.84 and
#   provides StateGraph, conditional cycles, Annotated reducers, Command
#   fan-out, checkpointers, get_state_history, interrupt() and
#   stream_mode="updates" — every capability the multi-agent work needs.
#   Revisit when ragas drops the langchain-community dependency.
langchain-core>=0.3.76,<1.0
langchain-openai>=0.3,<1.0
langchain-chroma>=0.2.6,<1.0      # 1.x declares langchain-core>=1.1.3
langchain-text-splitters>=0.3,<1.0
openai>=2.0,<3.0                  # instructor (pulled in by ragas) requires openai<3

# ── Graph orchestration ───────────────────────────────────────────────────────
langgraph>=1.0,<2.0
langgraph-checkpoint-sqlite>=2.0,<3.0   # 3.x needs langgraph-checkpoint>=4, which langgraph 1.0 forbids

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35

# ── Data ──────────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.4,<0.5
datasets>=2.20
langsmith>=0.3                    # tracing; set LANGSMITH_TRACING=true to enable

# ── Data ingestion ────────────────────────────────────────────────────────────
httpx>=0.27
beautifulsoup4>=4.12
requests>=2.31

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0
tiktoken>=0.7

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0
